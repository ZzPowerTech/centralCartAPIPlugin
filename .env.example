# Variáveis de Ambiente - CentralCart Top Plugin

## ⚠️ IMPORTANTE - NUNCA COMMITE ESTE ARQUIVO COM VALORES REAIS!

# Token de Autenticação da API CentralCart
# Obtenha seu token em: https://centralcart.com.br
CENTRALCART_API_TOKEN=COLOQUE_SEU_TOKEN_AQUI

# Exemplo de token (NÃO USAR EM PRODUÇÃO):
# CENTRALCART_API_TOKEN=7cf783d2-6142-4705-b207-e50b722735a8

## 📝 Instruções de Uso

### Para Desenvolvimento Local:

1. Copie este arquivo e renomeie para `.env`:
   ```bash
   cp .env.example .env
   ```

2. Edite o arquivo `.env` e coloque seu token real

3. O arquivo `.env` já está no `.gitignore` e nunca será commitado

### Para Produção (Servidor Minecraft):

Configure o token diretamente no arquivo de configuração do plugin:
`plugins/centralCartTopPlugin/config.yml`

```yaml
api:
  token: "SEU_TOKEN_AQUI"
```

## 🔐 Segurança

- ✅ Nunca compartilhe seu token publicamente
- ✅ Nunca commite o token no Git
- ✅ Use tokens diferentes para desenvolvimento e produção
- ✅ Revogue tokens antigos que não são mais usados
- ✅ Mantenha o `.gitignore` atualizado

## 🔄 Rotação de Tokens

Se você suspeitar que seu token foi exposto:

1. Gere um novo token no painel da CentralCart
2. Atualize o `config.yml` com o novo token
3. Reinicie o servidor Minecraft
4. Revogue o token antigo

